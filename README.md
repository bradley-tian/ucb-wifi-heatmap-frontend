# WiFi Issue Reporting Heatmap Interface
## 2023 UC Berkeley Mobile App Project

This is a frontend prototype of a WiFi issue display / report interface. All content should be viewed and implemented for testing purposes only. 

The startup commands have been configured in package.json to accommodate for environmental variables. To start this project in your local environment, run `npm start`. To build this project for production, run `npm run build`.
